import XCTest
@testable import InitialLettersView

final class InitialLettersViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(InitialLettersView().text, "Hello, World!")
    }
}
